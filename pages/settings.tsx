import { Layout } from "@/components";
import React from "react";

type Props = {};

export default function products({}: Props) {
  return (
    <Layout>
      <div>settings</div>
    </Layout>
  );
}
