import { Layout } from "@/components";
import React from "react";

type Props = {};

export default function categories({}: Props) {
  return (
    <Layout>
      <div>categories</div>
    </Layout>
  );
}
